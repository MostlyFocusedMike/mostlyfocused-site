{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node create-articles-metadata.js && vite",
    "build": "vite build",
    "work": "nodemon --no-deprecation create-articles-metadata.js --ignore './src'",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "nodemon": "^3.1.4",
    "vite": "^4.3.0"
  }
}
